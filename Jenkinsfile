pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'The build stage'
      }
    }
    stage('Unit Tests') {
      steps {
        echo 'Running Unit Tests'
      }
    }
    stage('Deploy') {
      steps {
        echo 'Deploy to stage'
      }
    }
  }
}